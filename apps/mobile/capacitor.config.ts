import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.myapp.mobile",
  appName: "com.myapp.mobile",
  webDir: "../web/out",
};

export default config;
